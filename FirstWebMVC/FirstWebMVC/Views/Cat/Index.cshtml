@model IEnumerable<FirstWebMVC.Models.Cat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.XuatXu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TuoiTho)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.XuatXu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TuoiTho)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GiongLoai">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GiongLoai">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GiongLoai">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
